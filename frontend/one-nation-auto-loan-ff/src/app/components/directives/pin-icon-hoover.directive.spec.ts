import { PinIconHooverDirective } from './pin-icon-hoover.directive';

describe('PinIconHooverDirective', () => {
  it('should create an instance', () => {
    const directive = new PinIconHooverDirective();
    expect(directive).toBeTruthy();
  });
});
